source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in action_view_preview.gemspec.
gemspec

group :development do
  gem 'pg'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
